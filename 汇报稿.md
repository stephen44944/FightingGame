# 🎮 双人横版对战游戏 — 8分钟汇报稿

> 预计总时长：约 7 分 30 秒（含操作缓冲）

---

## 第1页 · 封面（15秒）

大家好，我今天汇报的项目是**双人横版对战游戏**。

这是一个基于 Python 和 Pygame 开发的本地双人实时格斗游戏，采用面向对象设计，实现了状态机架构、精灵系统和多种设计模式。

---

## 第2页 · 目录（15秒）

汇报分为三个部分：
1. **项目背景**——为什么要做这个项目
2. **详细设计**——类结构和技术难点
3. **系统演示**——功能展示

---

## 第3页 · 项目背景（60秒）

首先是项目背景。

这个项目的动机有三个方面：
- 第一，这是 Python 课程的大作业，我希望通过它来**实践面向对象编程**；
- 第二，我从小喜欢玩拳皇这类格斗游戏，所以选择了游戏方向；
- 第三，双人同屏交互和实时碰撞检测是很有**技术挑战**的话题。

技术选型上，我选择了 **Python 3.8+** 加 **Pygame 2.0+**，这个组合的优势是简洁高效、无需额外依赖，可以专注于游戏逻辑本身。

整个项目虽然是一个学习项目，但具备了商业游戏的核心要素：8 名角色、近战远程双攻击类型、被动技能系统、可破坏地形、7 种特殊地图元素、道具系统和图片缓存优化。

---

## 第4页 · 游戏流程（45秒）

游戏的流程分为五个阶段：启动 → 主菜单 → 角色选择 → 实时对战 → 结算界面。

对战阶段有几个核心机制：
- 地图高度 3600 像素，可视区域是 800×600；
- 地图持续向上滚动，模拟危险区域逼近，被卷下去就死亡；
- 双方各只有 1 条命，HP 归零就结束；
- 地图里分布着可破坏砖块和增益道具；
- 帧率锁定在 60FPS，同时支持鼠标和键盘操作。

---

## 第5页 · 详细设计——类结构（60秒）

进入第二部分——详细设计。

项目的代码结构遵循**关注点分离**原则。入口 `gayforce.py` 只做启动，核心调度在 `data/main.py` 的状态机里。

整体架构分为三层：
- **tools.py** 中的 `Control` 类负责主循环和状态切换；
- **states/** 目录下是四个状态：主菜单、角色选择、对战和结算；
- **components/** 是游戏组件层，包含角色、子弹、砖块、道具等。

设计上用了两个核心模式：
- **状态模式**：`Control` 类调度四个 `_State` 子类，实现了 `MainMenu → Choosing → Gaming → GameOver` 的流转；
- **模板方法模式**：`Hero` 基类定义了 `update()` 的骨架流程，每个子类只需覆写 `action()` 和 `skill()` 方法。

角色继承树方面，所有角色继承自 `Hero`，再分成 `Warrior` 近战和 `Shooter` 远程两个分支。关羽、K 等 6 个是近战，Darling、弓箭手、蜘蛛王子是远程。

---

## 第6页 · 技术难点（60秒）

接下来谈谈四个技术难点。

**第一个是性能优化。** 原代码每帧都从磁盘 `pygame.image.load()` 加载图片，导致严重卡顿。我的方案是实现 `ImageCache` 全局缓存类，在启动阶段预加载所有角色动画帧和砖块素材，运行时零 I/O，帧率提升了 30% 到 50%。

**第二个是被动技能系统。** 8 个角色需要差异化能力，难点在于如何优雅扩展而不修改 `Hero` 基类。我采用了钩子方法模式：定义 `PassiveSkill` 基类，提供 `on_damage_dealt`、`on_damage_received`、`on_kill` 等钩子，8 个子类各自覆写。`Hero` 在 `update()` 中调用这些钩子即可。

**第三个是碰撞检测。** 子弹、剑、角色、砖块、道具、特殊元素之间是多对多关系。我用 Pygame 的 `sprite.spritecollide` 分组检测——子弹碰砖块就破坏或反弹，剑碰角色就触发伤害计算和被动技能。

**第四个是地图滚动。** 3600 像素超高地图需要持续向上滚动，同时保证双人视野。方案是定时滚动加 viewport 裁剪，屏幕外的砖块自动回收释放内存，并根据 viewport 位置动态生成新砖块。

---

## 第7页 · 特殊地图元素（45秒）

在改进中我还新增了 **7 种特殊地图元素**，用来增加游戏的互动性和策略性。

它们都继承自 `SpecialBrick` 基类，通过 `ActOnCharacter()` 方法影响角色：

- 传送门：站上去传送到配对传送门；
- 弹跳板：站上去弹飞；
- 治疗区：持续回复 HP；
- 加速带：获得 2.5 倍加速；
- 冰面：滑行效果，方向难控；
- 陷阱地板：持续扣血；
- 移动平台：自动左右移动。

在 `gaming.py` 的 `check_special_bricks_collisions()` 中每帧检测碰撞，触发对应效果。

---

## 第8页 · 系统演示——角色（45秒）

现在进入第三部分——系统演示。

游戏提供了 **8 名可选角色**，分为近战和远程两类。

近战角色血厚：关羽 1000 HP，K 有 800，Poena 有 700；远程角色血量低但灵活：弓箭手 250，Darling 和蜘蛛王子各 200，Iccy 200，Ghost 只有 150。

每名角色还有独特的**被动技能**，比如关羽的「武圣」击杀回血、K 的「闪避大师」20% 闪避、Darling 的「狂暴之心」低血量时伤害提升 50% 等等。这些被动技能大大增强了角色差异化和策略深度。

---

## 第9页 · 操作说明（45秒）

操作方面，玩家 1 使用键盘左侧的 W/A/S/D 移动、J 攻击、L 放技能；玩家 2 使用方向键移动、小键盘 1 攻击、小键盘 3 放技能。

另外游戏还支持**全流程鼠标操作**——主菜单、角色选择、结算界面都可以用鼠标悬停和点击，降低了上手门槛。

地图系统包含 8 种常规砖块材质和 7 种特殊元素，砖块被破坏后会掉落道具——HP 药水、MP 药水、加速鞋、护甲等 6 种。

---

## 第10页 · 功能亮点（30秒）

总结一下这个项目的六个功能亮点：

1. 被动技能系统增强策略深度；
2. 图片缓存优化大幅提升流畅度；
3. 7 种特殊地图元素增加互动性；
4. GIF 动画系统——每个角色有站立、行走、攻击、技能四套帧动画；
5. 鼠标全流程支持；
6. BGM 背景音乐系统。

---

## 第11页 · 总结与展望（30秒）

最后做个总结。

这个项目完成了完整的双人对战游戏，应用了状态模式、策略模式和模板方法模式实现可扩展架构。代码 2200 多行，517 个资源文件。项目已上传到 GitHub。

后续可以从连招系统、天气系统、网络对战、AI 对手、音效系统和角色升级等方向继续扩展。

以上就是我的汇报，谢谢大家！欢迎提问。

---

> **时间分配总结：**
> - 封面+目录：30秒
> - 项目背景（2页）：1分45秒
> - 详细设计（3页）：2分45秒
> - 系统演示（3页）：2分
> - 总结：30秒
> - **合计：约 7 分 30 秒**
